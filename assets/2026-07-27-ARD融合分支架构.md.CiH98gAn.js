import{_ as a,o as e,c as t,a2 as n}from"./chunks/framework.BOl2Vi4W.js";const E=JSON.parse('{"title":"ARD 调试器融合项目架构介绍","description":"","frontmatter":{},"headers":[],"relativePath":"2026-07-27-ARD融合分支架构.md","filePath":"2026-07-27-ARD融合分支架构.md"}'),i={name:"2026-07-27-ARD融合分支架构.md"};function p(l,s,r,o,h,c){return e(),t("div",null,[...s[0]||(s[0]=[n(`<h1 id="ard-调试器融合项目架构介绍" tabindex="-1">ARD 调试器融合项目架构介绍 <a class="header-anchor" href="#ard-调试器融合项目架构介绍" aria-label="Permalink to &quot;ARD 调试器融合项目架构介绍&quot;">​</a></h1><blockquote><p>项目：Async Rust Debugger（ARD）<br> 分支状态：<code>async-integration/async-debug</code> 当前工作区<br> 文档日期：2026 年 7 月 27 日</p></blockquote><h1 id="_1-项目简介" tabindex="-1">1. 项目简介 <a class="header-anchor" href="#_1-项目简介" aria-label="Permalink to &quot;1. 项目简介&quot;">​</a></h1><p>ARD（Async Rust Debugger）面向 Rust 异步程序以及采用异步执行模型的操作系统环境，目标是在现有源码级调试体系上恢复传统调试器难以直接表达的异步执行状态。项目保留 VS Code、DAP、GDB/MI 和目标平台调试能力，并在 GDB 运行现场建立 Future poll 观测、运行事件记录、逻辑关系重建和当前状态恢复机制，使调试结果不再局限于某一时刻的物理机器栈。</p><p>Rust 异步函数在编译后被转换为实现 <code>Future</code> 的状态机。执行器通过反复调用 <code>poll</code> 推进 Future；当 Future 返回 <code>Pending</code> 时，当前物理调用栈会退出，而 Future 的状态保存在对象内存中。下一次被唤醒时，执行器可能从另一段物理调用路径再次进入 <code>poll</code>。传统 backtrace 能准确反映暂停时的物理栈，却无法单独回答某个 Future 已经历多少次 poll、当前处于哪个状态、曾调用过哪些子 Future，以及暂停时逻辑上的异步父子关系。</p><p>ARD 解决的核心问题不是扩展函数列表，而是建立从调试控制事实到异步执行语义的连续数据链：</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A[GDB 断点与运行事件] --&gt; B[Future poll 生命周期]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B --&gt; C[RuntimeEventGraph]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C --&gt; D[History 调用关系]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B --&gt; E[暂停时 TLS/CID/Future 事实]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&gt; F[SnapshotV1]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D --&gt; G[Observer / Execution Graph]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    F --&gt; H[DAP logical async frames]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G --&gt; I[Async Inspector]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    F --&gt; I</span></span></code></pre></div><p>这条链路包含两类互补结果。RuntimeEventGraph 与 History 记录运行期间已经发生的事实，回答“异步程序此前执行过什么”；SnapshotV1 从暂停现场恢复当前线程仍然活跃的异步路径，回答“此刻哪些 Future 正在形成逻辑执行链”。Async Inspector 与 DAP logical frames 是这些事实的消费层，不承担事件推断和关系生成职责。</p><p>项目面向的长期场景包括普通 Rust async 程序、Embassy 等异步嵌入式框架、Lilos 异步微控制器环境，以及 ReL4 异步内核与用户态协同调试。当前阶段已经在 x86_64 Embassy std testcase 上完成 external remote、RuntimeEventGraph、History、SnapshotV1、DAP logical frames 和 Execution Graph 的运行验证；ARM Lilos 与 RISC-V ReL4 尚处于后续环境扩展阶段。</p><h1 id="_2-整体架构设计" tabindex="-1">2. 整体架构设计 <a class="header-anchor" href="#_2-整体架构设计" aria-label="Permalink to &quot;2. 整体架构设计&quot;">​</a></h1><p>当前融合架构以 main 分支的既有主调试框架为控制基础，将异步运行状态观测能力纳入同一调试会话。系统不是两个调试器的并行拼接，也没有保留第二套 adapter、session 或 GDB 控制入口。所有用户操作仍由 <code>type: &quot;ardb&quot;</code> 的 DAP 会话处理，异步模块通过同一 GDB 进程内的 Python 扩展采集运行事实，再由同一 TypeScript session 提供给 DAP 和 Inspector。</p><p>主调试框架负责与目标程序交互的控制职责，包括：</p><ul><li>VS Code Debug Adapter Protocol 请求处理；</li><li>GDB/MI2 进程管理和命令调度；</li><li>源码断点、函数断点、continue、step、pause 和 stackTrace；</li><li>本地 launch、external remote launch 与原有 attach；</li><li>面向操作系统调试的 BreakpointGroups；</li><li>描述内核态、用户态及其切换行为的 OSStateMachine。</li></ul><p>融合后的异步能力负责运行状态观测与逻辑链恢复，包括：</p><ul><li>whitelist 管理和 RuntimeEvent breakpoint 安装；</li><li>Future poll enter/exit 生命周期记录；</li><li>CID、Future address、poll sequence 与 DWARF 状态元数据；</li><li>RuntimeEventGraph 与 History Tree；</li><li>Trace Root 和 Observer Tree；</li><li>SnapshotV1、关系投影和 DAP logical async frames；</li><li>Async Inspector 的 Execution Graph 数据消费与 Snapshot 请求缓存。</li></ul><p>整体职责可以表示为：</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    subgraph Control[统一调试控制平面]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        V[VS Code / DAP]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        S[GDBDebugSession]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        M[MI2 backend]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        G[GDB]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        BP[BreakpointGroups]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        OS[OSStateMachine]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        V --&gt; S --&gt; M --&gt; G</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        S --&gt; BP</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        S --&gt; OS</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    subgraph Observation[异步运行状态观测平面]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        RT[runtime_trace.py]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        RE[RuntimeEvent]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        RG[RuntimeEventGraph]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        HI[History / Observer]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        SN[SnapshotV1]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        IN[Async Inspector / DAP logical frames]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        RE --&gt; RT --&gt; RG --&gt; HI --&gt; IN</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">        RT --&gt; SN --&gt; IN</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    end</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G --&gt; RE</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    S &lt;--&gt; RT</span></span></code></pre></div><p>该设计遵循两个原则。</p><p>第一，调试控制与异步观测分离。continue、step、远程连接、断点生命周期和 inferior 状态由主调试框架统一管理；<code>runtime_trace.py</code> 不创建另一条 launch 流程，不接管 QEMU 或 gdbserver，也不替代 MI2。这样能够保持现有 OS 调试骨架、BreakpointGroups 和 OSStateMachine 的行为边界。</p><p>第二，历史事实与当前状态分离。RuntimeEventGraph 是累计事实存储，History 和 Observer 从它投影；SnapshotV1 则读取暂停时的 per-thread live facts。两条数据链共享 RuntimeEvent observer 提供的 CID、符号和 Future 信息，但 Snapshot 不从 History JSON 反向生成，History 也不依赖 Snapshot 是否成功。</p><p>融合过程中，Debugger-2 的价值体现在异步运行观测设计和相关数据模型，而不是其旧调试入口。旧 adapter/session、testcase launch、独立 GDB 控制逻辑和旧 async tree 流程没有进入当前架构。对于 Embassy、Lilos 和 ReL4，旧仓库的构建参数、QEMU 参数、ELF 路径和 gdbstub 启动方式可作为 testcase 环境资料，ARD 调试会话仍由当前统一架构负责。</p><h1 id="_3-系统核心数据流" tabindex="-1">3. 系统核心数据流 <a class="header-anchor" href="#_3-系统核心数据流" aria-label="Permalink to &quot;3. 系统核心数据流&quot;">​</a></h1><h2 id="_3-1-调试控制链" tabindex="-1">3.1 调试控制链 <a class="header-anchor" href="#_3-1-调试控制链" aria-label="Permalink to &quot;3.1 调试控制链&quot;">​</a></h2><p>调试控制链负责把 VS Code 中的用户操作转化为 GDB/MI 命令，并将目标停止、线程、栈帧和断点结果返回给编辑器。其稳定性决定所有异步观测是否具备可靠运行现场。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sequenceDiagram</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant U as VS Code 用户界面</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant D as DAP Adapter</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant S as GDBDebugSession</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant M as MI2 backend</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant G as GDB</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant T as Target / gdbserver</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    U-&gt;&gt;D: launch / attach / breakpoint / continue / step</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D-&gt;&gt;S: DAP request</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    S-&gt;&gt;M: MI command</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    M-&gt;&gt;G: -break-insert / -exec-continue / -exec-step</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G-&gt;&gt;T: 本地控制或 target remote</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    T--&gt;&gt;G: breakpoint hit / signal / thread stop</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G--&gt;&gt;M: MI stopped record</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    M--&gt;&gt;S: structured event</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    S--&gt;&gt;D: DAP stopped / stackTrace / variables</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D--&gt;&gt;U: 编辑器与调试视图更新</span></span></code></pre></div><p><code>GDBDebugSession</code> 是统一会话协调者。它处理 launch/attach 配置，维护当前 transport、inferior 是否已经启动、OS 调试是否启用，并把源码断点映射为 GDB breakpoint。MI2 backend 负责 GDB 子进程、MI token、结果解析和命令顺序，不包含 RuntimeEventGraph 或 Snapshot 业务语义。</p><p>本地 launch、external remote launch 和 attach 共用同一 session，但传输与 OS 调试状态显式分离：</p><table tabindex="0"><thead><tr><th>模式</th><th>Transport</th><th>目标生命周期</th><th>OS 调试</th><th>首次运行语义</th></tr></thead><tbody><tr><td>local launch</td><td>local</td><td>ARD/GDB 启动本地程序</td><td>关闭</td><td><code>exec-run</code></td></tr><tr><td>external remote launch</td><td>remote</td><td>外部 testcase 拥有</td><td>关闭</td><td><code>exec-continue</code></td></tr><tr><td>原有 attach</td><td>remote</td><td>ARD 按 attach 配置启动 QEMU</td><td>开启</td><td><code>exec-continue</code></td></tr></tbody></table><p>external remote launch 通过明确的 <code>transport=remote</code> 与 <code>enableOsDebug=false</code> 表达“连接既有 gdbserver，但不启用 OS 切换逻辑”。原 attach 使用 <code>transport=remote</code> 与 <code>enableOsDebug=true</code>，继续初始化 OSStateMachine 和 BreakpointGroups。该分离避免通过是否存在 attach 配置间接推断 transport 或 OS 语义。</p><h2 id="_3-2-异步观测链" tabindex="-1">3.2 异步观测链 <a class="header-anchor" href="#_3-2-异步观测链" aria-label="Permalink to &quot;3.2 异步观测链&quot;">​</a></h2><p>异步观测链存在的原因，是 Future 的逻辑生命周期不能完全由普通源码断点推导。ARD 根据 whitelist 在 GDB 内安装内部 RuntimeEvent breakpoint，使每次目标 poll 命中时都能形成结构化事实。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    W[workspace temp/poll_functions.txt] --&gt; I[安装 RuntimeEventBP]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    I --&gt; H[poll symbol 命中]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    H --&gt; P[FuturePointerResolver 读取首参数]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    P --&gt; C[按 symbol + Future address 分配 CID]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C --&gt; Q[更新 poll sequence / state metadata]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    Q --&gt; E[_record_call_enter]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&gt; S[推入 per-thread TLS stack / call stack]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    S --&gt; R[程序执行 poll body]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    R --&gt; F[FinishBreakpoint]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    F --&gt; X[_record_call_exit]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    X --&gt; O[弹出 live stack]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&gt; G[RuntimeEventGraph]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    X --&gt; G</span></span></code></pre></div><p>runtime_trace 判断“哪个 Future 进入 poll”时，首先取得命中的 poll symbol，再通过 <code>FuturePointerResolver</code> 按目标 ABI 读取 poll 的第一个参数。对当前已验证的 x86_64 SysV 环境，该参数来自 <code>$rdi</code>。随后系统以 poll symbol 与 Future address 的组合建立协程实例身份，并分配 CID。将 symbol 纳入身份键可以避免不同 poll 语义因共享或接近的对象地址而被错误合并。</p><p>poll 进入时，observer 增加该 CID 的 poll sequence，采集可获得的 DWARF Future type 与状态字段，记录 <code>call_enter</code>，并把 CID 压入当前 GDB thread 对应的 TLS 逻辑栈。这里的 TLS stack 是调试器维护的 per-thread 异步嵌套状态，不等同于目标机器的 native stack。</p><p>poll 退出由内部 FinishBreakpoint 观察。退出事件写入 <code>call_exit</code>，同时从 call stack 和 TLS stack 中移除相应 CID。该设计使 History 可以保留已经发生的 enter/exit，而 Snapshot 仅在 poll 尚未退出的暂停窗口中看到活跃 CID。</p><p>父子关系主要通过同线程 call stack 建立。当子 poll 命中时，当前 call stack 顶部提供父 RuntimeEvent；系统记录 parent/child、CID、线程、事件编号及来源。对于需要更强 await 语义的关系，ARD 还可以利用父 Future 的活动变体、awaitee address/type 和 runtime child hit 进行验证，将通过校验的关系写入有界 RelationStore。RuntimeEventGraph 的执行边与 Snapshot 中的 observed await relation是不同强度的事实：前者表示运行时调用拓扑，后者要求更严格的 Future 关系证据。</p><h2 id="_3-3-历史调用链" tabindex="-1">3.3 历史调用链 <a class="header-anchor" href="#_3-3-历史调用链" aria-label="Permalink to &quot;3.3 历史调用链&quot;">​</a></h2><p>RuntimeEventGraph 保存调试会话期间已接受的运行事件，包括节点、边、根、enter/exit 计数、活动计数、事件序列和部分关系注释。其作用是使 Future suspend 后已经消失的物理调用关系仍能被查询。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E1[call_enter: parent Future] --&gt; N1[History node: parent]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E2[call_enter: child Future] --&gt; N2[History node: child]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E3[parent candidate accepted] --&gt; ED[History edge: parent -&gt; child]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E4[call_exit] --&gt; CT[更新 exit/active 计数]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    N1 --&gt; HT[History Tree]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    N2 --&gt; HT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ED --&gt; HT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    CT --&gt; HT</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    HT --&gt; OB[按 Trace Root 生成 Observer subtree]</span></span></code></pre></div><p>History Tree 表示已经发生过的执行关系。节点不会因为一次 poll 返回就从 History 中删除；相应的 active count 和 exit count会发生变化。这样既能描述“曾经调用”，也能标识查询时仍处于活动状态的节点。</p><p>History 与 Snapshot 在时间语义上严格区分：</p><ul><li>History 面向运行期间累计事实，可以在相关 Future 已 suspend、物理栈已退出后继续保留节点和边；</li><li>Snapshot 面向单次 GDB stop，只读取当前线程 live TLS path；</li><li>History 不由 Snapshot 生成，也不会因为 Snapshot 为空而丢失；</li><li>Snapshot 不从 History 猜测当前活跃节点，避免把过去事件误表示为当前状态。</li></ul><p>Async Inspector 的 Execution Graph 并不直接接收完整 History JSON。<code>ardb-get-observer-tree</code> 在 GDB 端读取同一 RuntimeEventGraph export，然后按 <code>ACTIVE_TRACE_ROOT</code> 选择一个 detached subtree。Panel 对该 subtree 做展示字段归一化，再发送到 webview。这一设计使 History 作为完整事实库，Observer 作为用户当前关注范围，两者职责明确。</p><h2 id="_3-4-当前状态恢复链" tabindex="-1">3.4 当前状态恢复链 <a class="header-anchor" href="#_3-4-当前状态恢复链" aria-label="Permalink to &quot;3.4 当前状态恢复链&quot;">​</a></h2><p>SnapshotV1 解决的是暂停瞬间的异步执行状态恢复。它必须在相关 poll 进入 observer 已执行、但 poll return observer 尚未执行的时间窗口内获取数据。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ST[GDB stop] --&gt; TH[确定 selected thread]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    TH --&gt; TLS[复制该线程 TLS stack]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    TLS --&gt; CID[逐个读取 CID metadata]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    CID --&gt; FA[Future address / poll sequence]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    FA --&gt; DW[DWARF Future type / state / source]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    DW --&gt; RP[RelationStore 只读投影]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RP --&gt; SV[SnapshotV1 root-to-leaf async_path]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    SV --&gt; RB[RuntimeTraceBridge 解析和校验]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RB --&gt; LF[DAP leaf-first logical async frames]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    RB --&gt; IC[Async Inspector Snapshot cache]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    LF --&gt; DS[与 MI physical frames 合并为 stackTrace]</span></span></code></pre></div><p>Snapshot builder 从 selected thread 对应的 <code>_TLS_STACK</code> 复制 CID 序列，然后为每个 CID生成节点。节点包含 Future address、Future type、poll sequence、state、status、diagnostics、源码位置、active 标志以及 <code>relation_from_parent</code>。relation projector 不修改 RelationStore，也不创造未经验证的 observed await；找不到匹配关系时会显式输出 <code>kind=unknown</code>、<code>confidence=unknown</code> 和空 evidence。</p><p>SnapshotV1 的 <code>async_path</code> 采用 root-to-leaf 顺序，适合表达异步父子结构。DAP stackTrace 需要把当前活动叶节点置于顶部，因此 <code>GDBDebugSession</code> 将该路径反转为 leaf-first logical frames，再在其后追加 MI 查询得到的 physical frames。Snapshot node 的 <code>physical=false</code> 表示逻辑帧；SnapshotV1 协议本身不包含 physical stack，物理栈仍由 GDB/MI提供。</p><p>History 表示过去，Snapshot 表示当前。二者结合后，调试者既能查看某条 async 链在运行期间的累计关系，也能在断点暂停时定位当前 active Future 和逻辑父节点。</p><h1 id="_4-核心功能介绍" tabindex="-1">4. 核心功能介绍 <a class="header-anchor" href="#_4-核心功能介绍" aria-label="Permalink to &quot;4. 核心功能介绍&quot;">​</a></h1><h2 id="_4-1-runtimeevent-异步事件捕获" tabindex="-1">4.1 RuntimeEvent 异步事件捕获 <a class="header-anchor" href="#_4-1-runtimeevent-异步事件捕获" aria-label="Permalink to &quot;4.1 RuntimeEvent 异步事件捕获&quot;">​</a></h2><p>RuntimeEvent 捕获机制是所有异步恢复能力的事实入口。它存在的原因是普通用户断点无法覆盖每次 Future poll，也无法在不改变程序源码的情况下持续记录 poll enter/exit。ARD 使用 workspace 的 whitelist 明确选择观测符号，在 GDB 中为这些符号安装内部 observer。</p><p>whitelist 承担运行观测边界控制。Rust 工程及其依赖可能包含大量 generic poll、执行器内部 helper 和同步函数；如果对所有候选符号安装 observer，会造成断点数量和事件噪声快速增长。当前机制支持 grouped whitelist 用于符号分类与选择，同时运行时加载 flat whitelist，确保最终 observer 集合可审查、可重复。</p><p>每个 RuntimeEvent 至少包含符号、CID、Future address、线程、poll sequence、程序计数器、事件来源和 whitelist admission 结果。进入事件写入 <code>call_enter</code>；poll 返回后写入 <code>call_exit</code>。父事件仍在同线程 call stack 时，子 poll 可形成执行边。该机制不依赖 testcase 脚本注入 breakpoint，也不要求修改被调试 Rust 源码。</p><p>Embassy std 验证使用最小 whitelist：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tick::__run_task::__run_task_inner_function::{async_fn#0}</span></span>
<span class="line"><span>embassy_time::timer::{impl#5}::poll</span></span></code></pre></div><p>运行结果形成：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>tick::__run_task::__run_task_inner_function::{async_fn#0}</span></span>
<span class="line"><span>  -&gt; embassy_time::timer::{impl#5}::poll</span></span></code></pre></div><p>observer 数量、符号解析、真实命中、call enter/exit 和 RuntimeEventGraph 节点/边均已完成运行验证。这说明 runtime_trace 能在真实外部 workspace 中识别用户 task Future 与 framework Future，而不是仅在内置 minimal testcase 上工作。</p><h2 id="_4-2-history-tree-调用关系重建" tabindex="-1">4.2 History Tree 调用关系重建 <a class="header-anchor" href="#_4-2-history-tree-调用关系重建" aria-label="Permalink to &quot;4.2 History Tree 调用关系重建&quot;">​</a></h2><p>History Tree 解决异步执行关系在 Future suspend 后无法由当前物理栈继续观察的问题。传统 backtrace 对同步调用十分准确，但 Future 返回 <code>Pending</code> 后，相应函数帧退出，后续暂停点的物理栈可能主要由 executor、waker 和调度循环构成。若仅查看当下 backtrace，调试者无法完整了解此前出现过的 async parent/child。</p><p>ARD 在事件发生时写入 RuntimeEventGraph，而不是在查询 History 时依据符号名称临时推断。节点进入次数、退出次数、活动状态和边来源均来自真实运行事件。History export 将图结构组织为可查询的 roots、nodes、edges 和 events，并保留稳定 root 语义。Observer 再按 Trace Root 裁剪为当前关注子树。</p><p>这一功能的价值在于把“运行时发生过的调用关系”从物理栈生命周期中独立出来。调试者可以在 Future 已 suspend 后继续观察其历史子调用，并将 Execution Graph 与当前 Snapshot 对照。History 不替代 physical backtrace；它提供的是异步逻辑时间维度上的累计结构。</p><h2 id="_4-3-snapshotv1-当前异步状态恢复" tabindex="-1">4.3 SnapshotV1 当前异步状态恢复 <a class="header-anchor" href="#_4-3-snapshotv1-当前异步状态恢复" aria-label="Permalink to &quot;4.3 SnapshotV1 当前异步状态恢复&quot;">​</a></h2><p>SnapshotV1 用于恢复 GDB stop 时仍活跃的 Future 路径。其输入是 per-thread TLS stack、CID metadata、Future address、poll sequence、DWARF type/state 和已验证关系记录。输出采用版本化协议，包含 session、generation、thread、empty 标志、transition、privilege 以及 async path。</p><p>Snapshot 的存在使调试器能够区分三种情况：当前没有活跃异步 poll；当前只有一个 active Future；当前存在父 Future 调用子 Future 的嵌套 poll。失败或暂不支持的元数据不会被填入默认成功值，而是通过 <code>status</code>、<code>error</code>、<code>source</code> 和 <code>confidence</code> 表达。</p><p>Embassy 在 <code>embassy-time/src/timer.rs:189</code> 暂停时，live TLS stack 为 <code>[1,2]</code>，SnapshotV1 成功恢复：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>run_task CID 1</span></span>
<span class="line"><span>  -&gt; Timer::poll CID 2 (active leaf)</span></span></code></pre></div><p>run-task Future 的 environment type和 <code>state=0</code> 由 DWARF 成功解析。Timer 节点身份、地址、poll sequence和 active 状态恢复成功；当前 state resolver 不覆盖其 <code>impl Future::poll</code> 符号形式，因此 Timer 的 type/state diagnostics 明确为 unsupported/unknown。首次 Timer poll 时 RelationStore 尚无 validator-approved record，子关系为 unknown，但两层 async path 已正确形成。报告对路径恢复与 observed await 关系强度进行了区分。</p><p>同一暂停点的 DAP stackTrace 产生 2 个 logical async frames，并保留 10 个 physical GDB frames，总计 12 帧。该结果证明 SnapshotV1 已进入调试器标准栈消费链，而不是停留在独立 JSON 文件中。</p><h2 id="_4-4-async-inspector" tabindex="-1">4.4 Async Inspector <a class="header-anchor" href="#_4-4-async-inspector" aria-label="Permalink to &quot;4.4 Async Inspector&quot;">​</a></h2><p>Async Inspector 是面向用户的异步状态展示与交互层。它存在的原因是 RuntimeEventGraph、Observer 和 SnapshotV1 均属于结构化调试数据，直接阅读 GDB console JSON 不适合作为日常调试入口。Inspector 将当前 Trace Root、Execution Graph、whitelist 候选和节点导航组织在同一 VS Code webview 中。</p><p>当前 Execution Graph 路径已完成运行验证：Panel 调用 <code>ardb-get-observer-tree</code>，归一化节点后发布 <code>updateTreeView</code> 与 <code>updateTree</code>，webview 把 treeData 渲染为 root/children。Embassy 的实际 payload 为 run-task root 和 Timer poll child，与 RuntimeEventGraph History 的节点、边和所选 Observer root 一致。</p><p>Snapshot 按钮当前直接调用 <code>ardb-get-snapshot</code>，通过 RuntimeTraceBridge 获得 SnapshotV1，输出结构化日志并保存 <code>_lastSnapshot</code>，供 logical frame 选择与节点导航使用。当前生产 UI 没有把 Snapshot 发布为独立可见树：Snapshot 点击不会发送 <code>updateTree</code>，也不会调用已存在但未接入活动路径的 <code>buildCurrentExecutionForest()</code>。因此应准确表述为“Inspector 已消费 Snapshot 数据，Execution Graph 已可视化；Snapshot 独立树视图仍是后续展示扩展”。</p><p>这一边界不影响核心数据链的有效性。RuntimeEvent、History、Snapshot 和 DAP logical frames 的生成均在展示层之前完成；UI 不参与 CID 分配、关系建立或状态解析。后续增加 Snapshot 可见视图时，应消费既有 SnapshotV1，不应在前端重新实现异步关系推断。</p><h1 id="_5-external-remote-launch-设计" tabindex="-1">5. External Remote Launch 设计 <a class="header-anchor" href="#_5-external-remote-launch-设计" aria-label="Permalink to &quot;5. External Remote Launch 设计&quot;">​</a></h1><p>external remote launch 用于连接由 testcase 或目标平台工具提前启动的 gdbserver、QEMU gdbstub 或硬件调试服务器。引入该模式的原因是不同 Rust 异步运行环境拥有不同的启动方式：Embassy std 使用 native gdbserver；Lilos 使用 ARM QEMU；ReL4 使用特定 RISC-V QEMU、boot image、SMP 和 CPU 参数。若由 ARD 内部统一创建这些环境，会把调试器与 testcase 构建系统及平台启动参数绑定。</p><p>当前配置形式为：</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;type&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;ardb&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;request&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;launch&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;program&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;/path/to/app.elf&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;remote&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;localhost:1234&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">,</span></span>
<span class="line"><span style="--shiki-light:#005CC5;--shiki-dark:#79B8FF;">  &quot;gdbPath&quot;</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">: </span><span style="--shiki-light:#032F62;--shiki-dark:#9ECBFF;">&quot;gdb&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>其中 <code>program</code> 用作 ELF symbol file，<code>remote</code> 是已存在的 GDB server endpoint，<code>gdbPath</code> 允许选择目标所需的 GDB executable。ARD 不启动 QEMU，不启动 gdbserver，也不拥有 inferior lifecycle。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sequenceDiagram</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant TS as Testcase 启动脚本</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant GS as gdbserver / QEMU stub</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant VS as VS Code workspace</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant ARD as GDBDebugSession</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant MI as MI2</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    participant G as GDB</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    TS-&gt;&gt;GS: build target 并开放 :1234</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    VS-&gt;&gt;ARD: launch(program, remote, gdbPath)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ARD-&gt;&gt;MI: connect(cwd, ELF, remote)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MI-&gt;&gt;G: file-exec-and-symbols ELF</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    MI-&gt;&gt;G: target-select remote localhost:1234</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G-&gt;&gt;GS: 建立远程调试连接</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ARD-&gt;&gt;G: ardb-load-whitelist</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    VS-&gt;&gt;ARD: Continue</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    ARD-&gt;&gt;G: exec-continue</span></span></code></pre></div><p>该模式带来三项架构收益。</p><p>其一，目标启动与调试控制分离。testcase 脚本负责 build、镜像准备、QEMU/gdbserver 和端口；ARD 负责符号、断点、RuntimeEvent、History、Snapshot 和 Inspector。</p><p>其二，ARD 不绑定特定 OS 启动方式。新增平台时优先提供正确 ELF、source workspace、GDB executable 和 remote endpoint，不需要创建另一套 debug adapter。</p><p>其三，原有 local launch 与 attach 行为保持。local launch 继续使用 MI2.load 和首次 <code>exec-run</code>；attach 继续负责 QEMU、OSStateMachine 与 BreakpointGroups；external remote 使用 MI2.connect 和 <code>exec-continue</code>，同时关闭 OS debug。后续 ReL4 若需要 external transport 与 OS debug 的新组合，应在现有显式状态模型上评审扩展，而不是恢复旧 Debugger-2 session。</p><h1 id="_6-embassy-std-适配成果" tabindex="-1">6. Embassy std 适配成果 <a class="header-anchor" href="#_6-embassy-std-适配成果" aria-label="Permalink to &quot;6. Embassy std 适配成果&quot;">​</a></h1><p>Embassy std 是当前第一个完成融合架构运行验证的外部真实 workspace。目标程序为 <code>/home/user/embassy/examples/std/target/debug/tick</code>，运行环境是 x86_64 Linux native，testcase 侧使用 <code>gdbserver :1234</code>，ARD 从 <code>/home/user/embassy</code> workspace 通过 external remote launch 连接。</p><h2 id="runtimeevent" tabindex="-1">RuntimeEvent <a class="header-anchor" href="#runtimeevent" aria-label="Permalink to &quot;RuntimeEvent&quot;">​</a></h2><p>当前 flat whitelist 仅包含 run-task 与 Timer poll 两个符号。ARD 成功加载 2 个 observer，两个 breakpoint 均有效且完成地址解析。运行时捕获了真实 create、classify、ALLOW、call_enter，并在 RuntimeEventGraph 中记录 enter/exit 生命周期。</p><p>最终累计验证得到：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>RuntimeEventGraph / History</span></span>
<span class="line"><span>nodes = 2</span></span>
<span class="line"><span>edges = 1</span></span>
<span class="line"><span></span></span>
<span class="line"><span>tick::__run_task::__run_task_inner_function::{async_fn#0}</span></span>
<span class="line"><span>  -&gt; embassy_time::timer::{impl#5}::poll</span></span></code></pre></div><p>这证明 external workspace temp、whitelist、GDB Python runtime、Future pointer、CID 和图写入链可以共同工作。</p><h2 id="snapshot" tabindex="-1">Snapshot <a class="header-anchor" href="#snapshot" aria-label="Permalink to &quot;Snapshot&quot;">​</a></h2><p>为确保 Snapshot 查询处于有效 live window，验证没有使用存在多个 DWARF location 的 <code>tick.rs:8</code>，而是在 <code>embassy-time/src/timer.rs:189</code> 设置源码断点。该位置位于 Timer poll 进入之后、返回 <code>Pending</code> 之前。</p><p>命中后：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>TLS stack = [run CID 1, Timer CID 2]</span></span>
<span class="line"><span>SnapshotV1.version = 1</span></span>
<span class="line"><span>SnapshotV1.empty = false</span></span>
<span class="line"><span>async_path.length = 2</span></span></code></pre></div><p>Snapshot 恢复 run-task parent 与 active Timer leaf，并经 RuntimeTraceBridge 进入 DAP stackTrace。logical frame 顺序为 Timer、run-task，随后保留真实 physical frames。由此确认“GDB stop → TLS/CID/Future → SnapshotV1 → logical async frames”的数据链已成立。</p><h2 id="inspector" tabindex="-1">Inspector <a class="header-anchor" href="#inspector" aria-label="Permalink to &quot;Inspector&quot;">​</a></h2><p>Inspector 的 Trace Root 选择已成功设置为 run-task。停止时，History、Observer export 和 Panel 发布的 treeData 在节点身份及拓扑上保持一致，Execution Graph 展示：</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>run_task</span></span>
<span class="line"><span>  └─ Timer::poll</span></span></code></pre></div><p>Snapshot 按钮能够获取同一暂停点的非空 SnapshotV1，并将其完整缓存到 Inspector；当前未提供 Snapshot 独立可见树，因此不能宣称 Inspector 已显示 Timer/run-task Snapshot 列表。</p><p>Embassy 的验证结论应表述为：ARD 已完成从 external remote 连接、RuntimeEvent 捕获、RuntimeEventGraph/History 重建，到 Execution Graph 展示的运行闭环；同时完成从暂停现场、SnapshotV1 到 DAP logical frames和 Inspector Snapshot cache 的当前状态恢复闭环。若项目验收把“Snapshot 独立 webview 树”纳入完整 UI 闭环，该展示环节仍待后续接入。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart LR</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A[gdbserver :1234] --&gt; B[external remote launch]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B --&gt; C[RuntimeEvent observers]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C --&gt; D[Graph: 2 nodes / 1 edge]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D --&gt; E[History / Observer]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&gt; F[Execution Graph 已验证]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C --&gt; G[TLS stack: CID 1 -&gt; CID 2]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G --&gt; H[SnapshotV1 非空]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    H --&gt; I[DAP logical frames 已验证]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    H --&gt; J[Inspector cache 已验证]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    J -.待扩展.-&gt; K[Snapshot 独立可见树]</span></span></code></pre></div><h1 id="_7-当前支持能力与边界" tabindex="-1">7. 当前支持能力与边界 <a class="header-anchor" href="#_7-当前支持能力与边界" aria-label="Permalink to &quot;7. 当前支持能力与边界&quot;">​</a></h1><p>当前项目已经形成统一架构下可运行的异步调试基础。已完成能力包括 Rust async poll 符号观测、whitelist 控制、Future pointer 与 CID 身份管理、poll enter/exit 生命周期、RuntimeEventGraph 累计事实、History/Observer 投影、SnapshotV1 当前状态恢复、DAP logical/physical stack 合并、Execution Graph 展示，以及 Embassy std external remote 运行验证。</p><p>为了区分“代码存在”和“平台已验证”，当前能力可以按以下层级说明：</p><table tabindex="0"><thead><tr><th>能力</th><th>当前状态</th><th>验证范围</th></tr></thead><tbody><tr><td>DAP/GDB/MI 调试控制</td><td>已完成</td><td>local、attach 既有路径；external remote 已实跑</td></tr><tr><td>BreakpointGroups / OSStateMachine</td><td>保持主框架能力</td><td>本轮 Embassy 不启用 OS debug</td></tr><tr><td>RuntimeEvent poll 捕获</td><td>已完成并验证</td><td>minimal、Embassy x86_64</td></tr><tr><td>RuntimeEventGraph / History</td><td>已完成并验证</td><td>Embassy 2 nodes / 1 edge</td></tr><tr><td>Trace Root / Observer</td><td>已完成并验证</td><td>Embassy run-task subtree</td></tr><tr><td>SnapshotV1</td><td>已完成并验证</td><td>Embassy 两层 async path</td></tr><tr><td>DAP logical async frames</td><td>已完成并验证</td><td>Embassy 2 logical + 10 physical</td></tr><tr><td>Inspector Execution Graph</td><td>已完成并验证</td><td>Embassy run-task → Timer</td></tr><tr><td>Inspector Snapshot 请求/cache</td><td>已完成并验证</td><td>与直接 Snapshot 对象一致</td></tr><tr><td>Inspector Snapshot 独立可见树</td><td>尚未接入活动 UI</td><td>作为后续展示扩展</td></tr><tr><td>Lilos ARM runtime</td><td>尚未运行验证</td><td>后续跨架构阶段</td></tr><tr><td>ReL4 跨特权级 async 链</td><td>尚未融合验证</td><td>后续 OS 深化阶段</td></tr></tbody></table><p>当前边界不改变项目技术路线。Lilos 与 ReL4 的主要工作不应重新实现 RuntimeEventGraph、History 或 Snapshot，而应验证目标 ABI、GDB 架构、DWARF 可用性、QEMU/gdbstub 参数、source mapping 和停止点稳定性。</p><p>跨架构方面，Future pointer resolver 必须按目标 ABI读取首参数。x86_64 Embassy 已验证 <code>$rdi</code> 路径；ARM Cortex-M 与 RISC-V 需要分别确认寄存器约定和 GDB architecture name。跨特权级方面，ReL4 还需要把 kernel/user symbol、BreakpointGroups、OSStateMachine transition 事实与 async observer 数据关联，当前不能将 <code>privilege=unknown</code> 描述为已恢复的内核/用户态语义。</p><p>复杂 OS 场景还可能涉及优化构建下的 DWARF 缺失、同一源码行多个 location、PIE/重定位、多个线程或 CPU 上的 Future 调度、执行器 generic poll 噪声以及不同地址空间的符号加载。这些属于后续平台适配需要验证的工程条件。现有协议通过 status/error/source/confidence 保留失败诊断，能够在扩展过程中避免把未知状态误报为成功状态。</p><h1 id="_8-后续发展路线" tabindex="-1">8. 后续发展路线 <a class="header-anchor" href="#_8-后续发展路线" aria-label="Permalink to &quot;8. 后续发展路线&quot;">​</a></h1><p>后续路线的重点是扩展运行环境并验证既有异步数据链，不是建立新的 async debugger 或复制 Debugger-2 旧功能。</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">flowchart TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A[当前基线: Embassy x86_64 external remote] --&gt; B[Lilos ARM Cortex-M]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B --&gt; C[确认 gdb-multiarch / Cortex-M ABI]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    C --&gt; D[验证 QEMU lm3s6965evb gdbstub]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    D --&gt; E[验证 RuntimeEventGraph / History / Snapshot]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&gt; F[ReL4 RISC-V]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    F --&gt; G[确认 a0 Future pointer 与优化 DWARF]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    G --&gt; H[加载 kernel / user symbols]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    H --&gt; I[复用 BreakpointGroups / OSStateMachine]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    I --&gt; J[异步内核 + 特权级切换链路]</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    J --&gt; K[复杂多任务与跨域场景]</span></span></code></pre></div><p>第一阶段是 Lilos。testcase 侧应使用既定 ARM 构建目标和 QEMU <code>lm3s6965evb</code>/Cortex-M3 参数开放 gdbstub；真实 Lilos workspace 提供 ELF、源码和 <code>type: ardb</code> external remote 配置。ARD 侧重点验证 <code>gdb-multiarch</code> 选择、ARM 首参数寄存器、poll symbol 解析、源码映射，以及 RuntimeEventGraph、History 和 SnapshotV1 在 Cortex-M 上是否保持同一协议语义。通过该阶段可以确认异步观测链从 host x86_64 扩展到嵌入式跨架构环境。</p><p>第二阶段是 ReL4。testcase 启动层继续负责定制 RISC-V QEMU、<code>virt</code> machine、特定 CPU、SMP、boot image 和 <code>:1234</code> gdbstub。ARD 需要验证 RISC-V <code>$a0</code> Future pointer、release debug-info、内核与用户态符号装载、source root、异步 syscall或任务 Future 的 RuntimeEvent。随后在不改变 OSStateMachine 与 BreakpointGroups 架构的前提下，评估 external transport 如何启用既有 OS debug 事实，并把 privilege transition 作为 Snapshot/History 的可信元数据来源。</p><p>第三阶段是复杂场景深化，包括多线程 executor、多个 CPU、Future 跨线程唤醒、嵌套 async syscall、内核与用户态分别 suspend/resume，以及更长时间 History 的容量和查询策略。该阶段需要以 RuntimeEvent 的真实 thread/CPU/privilege 事实为基础，不能由 UI 或符号名称推断。</p><p>展示层后续可在不改变 SnapshotV1 协议的前提下恢复 Snapshot 独立视图，将 root-to-leaf Snapshot path转换为清晰的当前状态树或 logical frame列表，并保持与 Observer Execution Graph 分离。该扩展应继续遵循“后端负责事实与关系，前端负责展示和导航”的原则。</p><p>综上，ARD 当前已经建立统一调试控制、异步运行观测、累计 History、暂停态 Snapshot和 Execution Graph 的架构基础，并通过 Embassy std 证明两条核心数据链能够在真实 external workspace 中运行。后续工作的主要风险和投入集中在目标架构 ABI、平台启动、符号与 DWARF、特权级事实和复杂调度环境，而不是重新设计 RuntimeEventGraph 或 Snapshot 协议。</p>`,118)])])}const k=a(i,[["render",p]]);export{E as __pageData,k as default};
